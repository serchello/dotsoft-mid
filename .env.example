# В dev можно оставить пустым — используется прокси из vite.config.js (/api/dotsoft -> dotsoft.gr)
# В проде укажите свой backend-прокси, который сам ходит на dotsoft.gr (обход CORS)
VITE_API_BASE_URL=/api/dotsoft
